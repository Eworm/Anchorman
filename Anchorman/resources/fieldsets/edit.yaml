sections:
  main:
    fields:
      url:
        type: text
        mode: url
      collection:
        type: collections
        max_items: 1
      scheduling:
        type: text
        mode: number
        width: 25
      status:
        type: radio
        inline: true
        options:
          publish: Publish immediately
          draft: Save as draft
