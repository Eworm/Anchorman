sections:
  main:
    fields:
      set_feed_url:
        type: text
        mode: url
      feed_collection:
        type: collections
        max_items: 1
      feed_status:
        instructions: "What to do with new articles?"
        type: radio
        inline: true
        options:
          draft: Draft
          publish: Publish
