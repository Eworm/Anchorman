routes:
  /:
    as: addons.anchorman
    uses: index
  /edit/{feed}:
    as: addons.anchorman.edit
    uses: edit
  /create:
    as: addons.anchorman.create
    uses: create
  post@store:
    as: addons.anchorman.store
    uses: store
  /refresh_all:
    as: addons.anchorman.refresh_all
    uses: refresh_all
  /get_item_structure:
    as: addons.anchorman.get_item_structure
    uses: get_item_structure
